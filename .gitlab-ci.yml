include:
  - template: Jobs/Secret-Detection.gitlab-ci.yml
  - component: gitlab.com/nomograph/pipeline/rust-cli@v2.4.6
    inputs:
      binary_name: patinate
      project_description: "Render Strava activity into stylized heatmap posters and inline SVG. Real polyline-circle obfuscation. Privacy flags for shippable renders."
      audit_allow_failure: false
      crates_publish: true

stages: [test, build, sign, publish, release, deploy]

default:
  tags: [storr]
  interruptible: true
  retry:
    max: 2
    when:
      - runner_system_failure
      - stuck_or_timeout_failure

variables:
  CARGO_HOME: ${CI_PROJECT_DIR}/.cargo

# GitLab Pages: render the bundled fixture in --web mode and serve
# the editorial demo. Only deploys from main.
pages:
  stage: deploy
  image: registry.gitlab.com/nomograph/pipeline/rust-cli:latest
  cache:
    - key: cargo-registry-${CI_COMMIT_REF_SLUG}
      paths:
        - .cargo/registry/cache
        - .cargo/registry/index
      policy: pull-push
    - key: cargo-target-${CI_COMMIT_REF_SLUG}
      paths:
        - target/
      policy: pull-push
    - key: osm-${CI_PROJECT_ID}
      paths:
        - fixtures/grand-rapids.osm.json.gz
      policy: pull-push
  script:
    - cargo build --release
    - mkdir -p fixtures public
    # OSM fixture is fetched on first run, cached thereafter via the
    # osm-${CI_PROJECT_ID} cache key.
    - test -f fixtures/grand-rapids.osm.json.gz
        || ./target/release/patinate fetch-osm --city "Grand Rapids" --radius-m 25000
             --out fixtures/grand-rapids.osm.json.gz
    # Privacy: the demo renders fixtures/activities.json, which is
    # public Strava segment polylines (athlete IDs zeroed, gear IDs
    # placeholders, names generic). No real-rider identifiers ship.
    - ./target/release/patinate render
        --config fixtures/config.toml
        --osm fixtures/grand-rapids.osm.json.gz
        --activities fixtures/activities.json
        --theme cycle_heat
        --web --transparent-bg
        --out public/heatmap-web.svg
    - cp examples/site/index.html public/index.html
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
